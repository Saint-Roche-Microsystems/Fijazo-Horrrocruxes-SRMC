# Carga el exchange `bets.events`, la cola `progression.recalc` y su binding
# al arrancar, para que la topología esté declarada sin script manual.
load_definitions = /etc/rabbitmq/definitions.json

# Escucha AMQP en el puerto estándar.
listeners.tcp.default = 5672
